# Resume run for chunk 2 — only processes the PDFs listed in file_list
# (the ones that don't yet have an ocr_docling.json in output_dir).
#
# Generated 2026-06-11: 860 of 1406 chunk_2 PDFs were still pending
# (one, 16RA388, had a partial/incomplete output dir and is included
# so it gets redone).

# Input — directory containing the chunk 2 PDFs
pdf_input: 'D:\Local_repository\2024_forms_chunked\chunk_2'

# Output directory — same as the original chunk 2 run, so completed
# PDFs land alongside the ones already done.
output_dir: 'D:\Local_repository\2024_forms_chunked\2024_forms_chunk_2_surya_docling_6_5_2026'

# Only process the files listed here (one filename/stem per line),
# resolved against pdf_input.
file_list: 'file_lists/chunk2_pending.txt'

# Extra safety net: re-check output_dir and skip any PDF that already
# has a complete ocr_docling.json, even if it's listed in file_list.
skip_existing: true

# Docling + Surya pipeline configuration
docling:
  markdown_dir: 'D:\Local_repository\2024_forms_chunk_2_surya_docling_6_5_2026'