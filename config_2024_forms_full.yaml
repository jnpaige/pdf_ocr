# Full reprocess of the 2024 site-form corpus (5,254 PDFs: the original
# 4,801 already-OCR'd forms, corrected via fresh do_ocr:true Surya OCR after
# the backfill path was found to corrupt text, plus 453 new 16VN-prefixed
# forms never OCR'd before) from their original, never-OCR'd source PDFs.
#
# output_dir already contains the 5-form validated pilot
# (16CL100/16CL101/16CL102_16CL191/16CL103/16CL104) — skip_existing leaves
# those alone and only processes the remaining ~5,249 PDFs.
pdf_input:  'C:\Users\jpaige\Desktop\local_repositories\2024_forms'
output_dir: 'C:\Users\jpaige\Desktop\local_repositories\2024_forms_ocr_docling_fresh_pilot'

skip_existing: true
